{
  "name": "universithi-suid-afrika",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/api-client.js && node --check scripts/verify-live-readiness.mjs && node --check scripts/smoke-live-data.mjs && node scripts/verify-live-readiness.mjs && node scripts/smoke-live-data.mjs"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "helmet": "^8.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
